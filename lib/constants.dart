const double defaultPadding = 16.0;

// SharedPrefs keys
const accessTokenKey = 'accessToken';
const refreshTokenKey = 'refreshToken';
const userIdKey = 'userId';

// GET params keys
const uniqueIdKey = 'uniqueId';
const codeKey = 'secretCode';
